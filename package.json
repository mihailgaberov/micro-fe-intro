{
  "private": true,
  "scripts": {
    "start": "concurrently \"cd addtocart && yarn start\" \"cd cart && yarn start\" \"cd home && yarn start\" \"cd pdp && yarn start \" \"cd server && yarn start:dev\"",
    "build": "concurrently \"cd addtocart && yarn && yarn build:start\" \"cd cart && yarn && yarn build:start\" \"cd home && yarn && yarn build:start\" \"cd pdp && yarn && yarn build:start\" \"cd server && yarn && yarn start:prod\""
  },
  "dependencies": {
    "concurrently": "^6.4.0"
  }
}
